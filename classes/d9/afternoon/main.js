import "./style.css";
import $ from "jquery";

const app = {

}

const handleAdd = () => {
  const text = $("input").val("testing")
  console.log(text)
}

const main = () => {
  // $("button").on("click", handleAdd)
};

$(main);
